/**
 * 라우트 전환 중에 보여주는 최소 로딩 표시.
 *
 * **정교한 스켈레톤을 만들지 않는다.** 화면마다 실제 배치를 흉내 낸 스켈레톤을
 * 두면 그 화면이 바뀔 때마다 스켈레톤도 같이 고쳐야 하고, 안 고치면 로딩 중과
 * 완성 화면의 모양이 어긋나서 오히려 더 튄다. 여기서 필요한 것은 "요청이
 * 진행 중이다"라는 신호 하나다.
 *
 * 이게 필요한 이유는 구조에 있다: 서버 컴포넌트가 자기 Route Handler 를 fetch
 * 하는 구조라 응답이 올 때까지 렌더가 시작되지 않고, loading.tsx 가 없으면
 * 그동안 화면이 이전 페이지에 멈춰 있어 클릭이 먹지 않은 것처럼 보인다.
 *
 * 각 라우트의 loading.tsx 가 이 컴포넌트를 그대로 쓴다. 문구를 라우트마다 다르게
 * 두지 않는 것은 사용자가 그 차이로 얻는 것이 없기 때문이다.
 *
 * 높이를 dvh 로 잡는다 (CLAUDE.md "뷰포트 높이는 dvh 를 사용한다. vh 금지").
 * 40dvh 는 헤더 아래에서 화면 중앙쯤에 오는 값이고, 전체 높이를 잡으면 푸터가
 * 로딩 중에만 저 아래로 밀려난다.
 */
export default function RouteLoading() {
  return (
    <div
      // 스크린리더에는 "불러오는 중"이 한 번 읽히면 된다. aria-live 를 쓰지 않는
      // 이유는 이 노드가 나타났다 사라지는 것 자체가 이미 상태 변화라서다.
      role="status"
      className="flex min-h-[40dvh] items-center justify-center px-4"
    >
      <p className="animate-pulse text-[16px] text-gray3">불러오는 중…</p>
    </div>
  );
}
